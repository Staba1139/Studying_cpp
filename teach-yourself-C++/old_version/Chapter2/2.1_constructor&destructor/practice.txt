1. 練習問題1.5の3. で作成したキュークラスを改良，初期化関数をコンストラクタに置き換えよ．
2. 経過時間を測定するストップウォッチを真似て，stopwatchクラスを作成．
   コンストラクタを使って初期の経過時間を0に設定
   start()とstop()という2つのメンバ関数を作成，経過時間を表示．
   さらにデストラクタ関数を作成して，stopwatchオブジェクトを破棄する際に経過時間を自動的に表示する．

3. 次に示すコンストラクタの誤りを指摘する．

class sample{
    double a, b, c;
    public:
        double sample();
};
